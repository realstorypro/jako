# source folder we're using
source: gravity

replace:
  url: www.radiant9.com
  name: radiant9

heorku:
  addons:
    - sendgrid:starter
    - scheduler:standard
    - papertrail:choklad
    - heroku-redis:hobby-dev
    - heroku-postgresql:hobby-dev
